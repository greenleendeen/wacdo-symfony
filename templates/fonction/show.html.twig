{% extends 'base.html.twig' %}

{% block title %}Fonction{% endblock %}

{% block body %}
    <h1>Fonction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>Intitule_poste</th>
                <td>{{ fonction.intitulePoste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fonction_index') }}" class= "smallButton" >revenir à la liste</a>

    <a href="{{ path('app_fonction_edit', {'id': fonction.id}) }}" class= "smallButton" >editer</a>

    {{ include('fonction/_delete_form.html.twig') }}
{% endblock %}

