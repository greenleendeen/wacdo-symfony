{% extends 'base.html.twig' %}

{% block title %}Affectation
{% endblock %}

{% block body %}
	<h1>Affectation</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ affectation.id }}</td>
			</tr>
			<tr>
				<th>Debut_contrat</th>
				<td>{{ affectation.debutContrat ? affectation.debutContrat|date('Y-m-d') : '' }}</td>
			</tr>
			<tr>
				<th>Fin_contrat</th>
				<td>{{ affectation.finContrat ? affectation.finContrat|date('Y-m-d') : '' }}</td>
			</tr>
		</tbody>
	</table>
	<div class="salut">
		<a href="{{ path('app_affectation_index') }}" class="smallButton">revenir à la liste</a>

		<a href="{{ path('app_affectation_edit', {'id': affectation.id}) }}" class="smallButton">editer</a>

	</div>

	{{ include('affectation/_delete_form.html.twig') }}

{% endblock %}
