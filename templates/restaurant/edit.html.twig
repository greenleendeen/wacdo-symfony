{% extends 'base.html.twig' %}

{% block title %}Editer Restaurant{% endblock %}

{% block body %}
    <h1>Editer</h1>

    {{ include('restaurant/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_restaurant_index') }}" class= "smallButton" >revenir à la liste</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
